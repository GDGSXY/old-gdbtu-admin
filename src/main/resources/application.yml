server:
  port: 18080
spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/gdbtu_admin
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
